# Google OAuth Configuration
# Get these from Google Cloud Console (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/callback

# GSC Configuration
GSC_CONFIG_PATH=./config/gsc-config.json
GSC_TOKENS_PATH=./config/gsc-tokens.json

# Audit Logging
AUDIT_LOG_PATH=./logs/audit.log

# MCP Transport Configuration
# Options: 'http' (for external connections) or 'stdio' (for CLI)
MCP_TRANSPORT=http

# HTTP Server Configuration (only used when MCP_TRANSPORT=http)
HTTP_PORT=3001
ALLOWED_ORIGINS=*
# For production, set specific origins: ALLOWED_ORIGINS=https://oma.wpp.com,https://app.wpp.com

# Development Mode (DANGEROUS - disable in production!)
# When enabled, allows bypassing OAuth with X-Dev-Bypass: true header
ENABLE_DEV_BYPASS=false

# Logging
LOG_LEVEL=INFO

# Optional: Google Ads Configuration
# GOOGLE_ADS_DEVELOPER_TOKEN=your-developer-token

# Optional: Bright Data SERP API
# BRIGHT_DATA_API_KEY=your-api-key
